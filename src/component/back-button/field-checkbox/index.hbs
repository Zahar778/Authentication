<div class="field field--checkbox">
	<button onclick="
	fieldCheckbox.toggle(this);
	{{action}}('{{name}}', this.hasAttribute('active'));.field__label--checkbox {
	font-size: 16px;
	color: s.color('dark');
	padding-top: 3px;
}
	"
	type="button" class="field__weapper">
		<span class="field__icon"></span>
		<span class="field__label field__label--checkbox">{{label}}</span>
	</button>
</div>